#include <iostream>

using namespace std;

int main()
{
	int samochod = 1024, automobile = 2048; 
	// auto - reserved word

	int wrti = wrti; 
	// random int in memory, but all good

	int wrti2(int()); 
	// don't know what it does, but it works ;)

	double pensja, zarobki = 9999.99; 
	// undefined variable zarobki, should be , instead of =

	int wartosc;
	cin >> wartosc; 
	// declare first, then assign
}